name: backend-dev-deploy

on:
  workflow_run:
    workflows: [backend-ci]
    types: [completed]
    branches: [main]
  workflow_dispatch:
    inputs:
      target_env:
        description: "Terraform deploy environment profile under infra/terraform/envs."
        required: true
        default: dev
        type: string
      apply:
        description: "Apply the Terraform plan. Leave false for plan-only validation."
        required: true
        default: false
        type: boolean

env:
  AWS_REGION: ap-northeast-2
  TF_DIR: infra/terraform
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

permissions:
  contents: read
  id-token: write

concurrency:
  group: backend-dev-deploy-main
  cancel-in-progress: false

jobs:
  deploy-dev:
    name: Deploy dev backend
    # Deploy only after backend-ci succeeded on main (or on explicit dispatch).
    if: >-
      github.event_name == 'workflow_dispatch' ||
      github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.target_env || 'dev' }}
    timeout-minutes: 35
    env:
      TARGET_ENV: ${{ github.event.inputs.target_env || 'dev' }}
    steps:
      # Deploy the exact commit backend-ci validated, not whatever main
      # points at when this workflow starts.
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.event.workflow_run.head_sha || github.ref }}

      - name: Resolve deploy profile
        id: deploy-profile
        env:
          GITHUB_VARIABLES_JSON: ${{ toJson(vars) }}
        run: python scripts/resolve_backend_deploy_profile.py >>"$GITHUB_OUTPUT"

      - uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: ${{ steps.deploy-profile.outputs.deploy_role_arn }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Verify deploy account matches Terraform backend
        env:
          DEPLOY_ROLE_ARN: ${{ steps.deploy-profile.outputs.deploy_role_arn }}
          TF_BACKEND_CONFIG: ${{ steps.deploy-profile.outputs.tf_backend_config }}
        run: scripts/verify_deploy_account_matches_backend.sh

      - name: Install uv
        uses: astral-sh/setup-uv@v8.2.0

      - name: Set up Python
        run: uv python install 3.13

      - name: Hydrate AgentCore Runtime metadata
        run: >-
          uv run python scripts/deploy_agentcore_runtime.py
          --hydrate-only
          --var-file "${{ env.TF_DIR }}/${{ steps.deploy-profile.outputs.tf_var_file }}"
          --region "${{ env.AWS_REGION }}"

      - name: Check AgentCore Runtime readiness
        run: >-
          scripts/check_agentcore_runtime_preflight.sh
          --terraform-dir "${{ env.TF_DIR }}"
          --var-file "${{ steps.deploy-profile.outputs.tf_var_file }}"
          --region "${{ env.AWS_REGION }}"

      - name: Build Lambda package
        run: ./scripts/package_api_lambda.sh

      - uses: hashicorp/setup-terraform@v4
        with:
          terraform_wrapper: false

      - name: Terraform init
        run: >-
          terraform init
          -input=false
          -backend-config="${{ steps.deploy-profile.outputs.tf_backend_config }}"
        working-directory: ${{ env.TF_DIR }}

      - name: Terraform plan
        run: >-
          terraform plan
          -input=false
          -var-file="${{ steps.deploy-profile.outputs.tf_var_file }}"
          -var='operational_alarm_email_addresses=${{ vars.OPERATIONAL_ALARM_EMAILS_JSON }}'
          -out=tfplan
        working-directory: ${{ env.TF_DIR }}

      - name: Terraform apply
        if: github.event_name == 'workflow_run' || inputs.apply == true
        run: terraform apply -input=false -auto-approve tfplan
        working-directory: ${{ env.TF_DIR }}

      - name: Direct deploy AgentCore Runtime
        if: github.event_name == 'workflow_run' || inputs.apply == true
        run: |
          runtime_role_arn="$(terraform -chdir="${TF_DIR}" output -raw agentcore_runtime_role_arn)"
          uv run python scripts/deploy_agentcore_runtime.py \
            --var-file "${TF_DIR}/${{ steps.deploy-profile.outputs.tf_var_file }}" \
            --region "${AWS_REGION}" \
            --role-arn "$runtime_role_arn"

      - name: Terraform plan after AgentCore direct deploy
        if: github.event_name == 'workflow_run' || inputs.apply == true
        run: >-
          terraform plan
          -input=false
          -var-file="${{ steps.deploy-profile.outputs.tf_var_file }}"
          -var='operational_alarm_email_addresses=${{ vars.OPERATIONAL_ALARM_EMAILS_JSON }}'
          -out=tfplan-after-agentcore
        working-directory: ${{ env.TF_DIR }}

      - name: Terraform apply after AgentCore direct deploy
        if: github.event_name == 'workflow_run' || inputs.apply == true
        run: terraform apply -input=false -auto-approve tfplan-after-agentcore
        working-directory: ${{ env.TF_DIR }}

      - name: Skip Terraform apply
        if: github.event_name == 'workflow_dispatch' && inputs.apply != true
        run: echo "Plan-only validation completed. Re-run with apply=true to apply this plan."
